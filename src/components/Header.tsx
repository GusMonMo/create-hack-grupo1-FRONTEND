import Navigation from './Navigation'

const Header = () => {
  return (
    <header className="flex justify-center items-center w-full h-max bg-primary shadow-sm">
        <Navigation />
    </header>
  )
}

export default Header
